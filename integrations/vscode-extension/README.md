# MyAI VS Code Extension (Starter)

This starter adds a command:

- `MyAI: Run Quick Command` (`myai.runQuickCommand`)

Use this scaffold to wire API calls to your deployed app:

- Endpoint example: `/api/integrations/chat-bot`
- Future MCP-aware endpoint: `/api/mcp/context`

